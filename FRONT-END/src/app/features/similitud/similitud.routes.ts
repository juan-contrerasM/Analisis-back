import { Routes } from '@angular/router';
import { SimilitudPageComponent } from './similitud-page.component';

export const SIMILITUD_ROUTES: Routes = [{ path: '', component: SimilitudPageComponent }];
