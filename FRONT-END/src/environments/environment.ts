export const environment = {
  production: true,
  apiUrl: 'http://localhost:8080',
  /** Cada cuánto se consulta estado y se recargan dataset / análisis (si el ETL está LISTO). */
  autoRefreshIntervalMs: 60_000,
};
